export const siteName = "Meirion Pritchard";

// Staging host pre-launch (D023). Swaps to https://meirionpritchard.com at the
// production cutover — see A1-9 staging notes.
export const siteUrl = "https://meirionpritchard.assoc.one";
